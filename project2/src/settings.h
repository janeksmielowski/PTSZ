#ifndef SETTINGS_H
#define SETTINGS_H

#include <iomanip>
#include <iostream>
#include <cstdlib>
#include <ctime>
#include <fstream>
#include <vector>
#include <windows.h>

const int INSTANCES = 10;
const int INSTANCE_SIZES[] = { 50, 100, 150, 200, 250, 300, 350, 400, 450, 500 };

const int GEN_MACHINE_PRECISION = 2;
const int GEN_MACHINE_RANGE = 5;
const int GEN_TASK_MAX_DURATION = 100;

#endif